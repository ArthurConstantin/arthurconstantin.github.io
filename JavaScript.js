//Modifier la fonction pour qu'elle prenne un paramètre, ce paramètre sera le nom du cookie
//vérifier si le cookie existe ou pas s'il existe pas on le créer et on affiche le block modal sinon on fait rien
// Il faut que le nom du cookie et la vérification de son de son existance soit variabilisé donc 
// je mets le nom dans une variable et je réutilise
const cookiesite = 'henri'

function createCookie(nomCookie) {
    let hours = 2;
    var expires = "";
    if (hours) {
    var date = new Date();
    date.setTime(date.getTime() + (hours * 60 * 60 * 1000));
    expires = "; expires=" + date.toUTCString();
    }
    document.cookie = nomCookie + "=" + (true) + expires + "; path=/";
    }


let modal = document.getElementsByClassName("modal");


function getCookie(cname) {
    let name = cname + "=";
    let decodedCookie = decodeURIComponent(document.cookie);
    let ca = decodedCookie.split(';');
    for(let i = 0; i <ca.length; i++) {
      let c = ca[i];
      while (c.charAt(0) == ' ') {
        c = c.substring(1);
      }
      if (c.indexOf(name) == 0) {
        console.log("là")
        return c.substring(name.length, c.length);
      }else{
        console.log("pas là")
      }
    }
    return "";
  }

  window.onload = function () {
    if (document.cookie.indexOf(cookiesite + "=") === -1) {
    console.log("jdsk")
    }
    };


  getCookie(cookiesite)

//let cookieverif = 
    function getCookie(nomCookie) {
        if (getCookie = false) { 
            createCookie(nomCookie)
            modal[0].style.display = "block"
        }else{
            modal[0].style.display = "none"
            }
        }

//if getCookie() {
    //createCookie(cookiesite) 
    ///modal[0].style.display = "block";
//}else{







let etoile = document.getElementsByClassName("etoile1")
let pascontent = document.getElementsByClassName("pascontent")

if (etoile1 = true) {
    pascontent[0].style.display = "block"
}

































































































































































































































































































































































































































































































































































































































































































































































































































